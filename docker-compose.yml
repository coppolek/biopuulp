version: '3.8'

services:
  web:
    build: .
    container_name: biolinkpro-web
    ports:
      - "80:3000"
    restart: always
    environment:
      - NODE_ENV=production
      # - GEMINI_API_KEY=your_key_here se dovesse servire in futuro
